<div class="field"><label class="field__label" for="report-account">Cuenta</label><select class="field__control" id="report-account" name="account"><option value="">Todas</option>@foreach($accounts as $account)<option value="{{ $account->id }}" @selected((string) ($filters['account'] ?? '') === (string) $account->id)>{{ $account->name }}{{ $account->archived_at ? ' (archivada)' : '' }}</option>@endforeach</select></div>
<div class="field"><label class="field__label" for="report-category">Categoría</label><select class="field__control" id="report-category" name="category"><option value="">Todas</option>@foreach($categories as $category)<option value="{{ $category->id }}" @selected((string) ($filters['category'] ?? '') === (string) $category->id)>{{ $category->name }}{{ $category->archived_at ? ' (archivada)' : '' }}</option>@endforeach</select></div>
<div class="field"><label class="field__label" for="report-member">Miembro</label><select class="field__control" id="report-member" name="member"><option value="">Todos</option>@foreach($members as $member)<option value="{{ $member->id }}" @selected((string) ($filters['member'] ?? '') === (string) $member->id)>{{ $member->name }}</option>@endforeach</select></div>
<div class="field"><label class="field__label" for="report-tag">Etiqueta</label><select class="field__control" id="report-tag" name="tag"><option value="">Todas</option>@foreach($tags as $tag)<option value="{{ $tag->id }}" @selected((string) ($filters['tag'] ?? '') === (string) $tag->id)># {{ $tag->name }}{{ $tag->archived_at ? ' (archivada)' : '' }}</option>@endforeach</select></div>
