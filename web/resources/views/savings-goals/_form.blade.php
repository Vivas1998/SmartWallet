<div class="form-grid">
    <div class="field"><label class="field__label" for="goal-name">Nombre</label><input class="field__control" id="goal-name" name="name" value="{{ old('name', $goal?->name) }}" maxlength="120" placeholder="Ej. Fondo de emergencia" required></div>
    <div class="field"><label class="field__label" for="goal-amount">Importe objetivo</label><div class="money-field"><input class="field__control money-field__control" id="goal-amount" name="target_amount" inputmode="decimal" value="{{ old('target_amount', $goal ? number_format($goal->target_amount_cents / 100, 2, ',', '.') : '') }}" required><span class="money-field__suffix">€</span></div></div>
    <div class="field"><label class="field__label" for="goal-account">Cuenta vinculada</label><select class="field__control" id="goal-account" name="financial_account_id" required><option value="">Selecciona una cuenta</option>@foreach($eligibleAccounts as $account)<option value="{{ $account->id }}" @selected((string) old('financial_account_id', $goal?->financial_account_id) === (string) $account->id)>{{ $account->name }} · {{ $account->type->label() }}</option>@endforeach</select><small class="field__hint">Solo cuentas de ahorro o inversión externa.</small></div>
    <div class="field"><label class="field__label" for="goal-date">Fecha objetivo <span class="field__optional">opcional</span></label><input class="field__control" id="goal-date" name="target_date" type="date" value="{{ old('target_date', $goal?->target_date?->toDateString()) }}"></div>
</div>
